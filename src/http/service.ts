import { IService } from "../interfaces.ts";
import * as Drash from "../../mod.ts"

export class Service implements IService {
    run(request: Drash.DrashRequest, response: Drash.DrashResponse) {

    }
}
