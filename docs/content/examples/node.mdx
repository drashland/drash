---
title: Node
description: Drash / Node — complete, runnable examples in ESM, CommonJS, and TypeScript.
---

# Drash / Node

There are three Node examples in the repository, differing only in module system and typing. Pick the tab that matches your project.

<Tabs items={["ECMAScript (ESM)", "CommonJS (CJS)", "TypeScript"]}>
  <Tabs.Tab>

## Steps

<Steps>

### Install Node

Install [Node](https://nodejs.org) (v20+).

### Install dependencies

Install the dependencies in `package.json`.

```bash
mkdir my-project
npm init -y
npm install @drashland/drash
```

### Create the following file

```js filename="app.js"
import {
  Chain,
  Resource,
} from "@drashland/drash/modules/http.polyfill.js";
import { createServer } from "node:http";

// Create a resource
class Home extends Resource {
  paths = ["/"];

  GET(context) {
    console.log(`Received request: ${context.request.url}`);
    context.response.end(`Oh so easy (written at ${new Date()})`);
  }
}

// Build the chain and add the resource
const chain = Chain
  .builder()
  .resources(Home)
  .build();

// Define server variables for reuse below
const hostname = "localhost";
const port = 1447;

// Create the server
const server = createServer((request, response) => {
  // Create a context object that the resource can use to access the request
  // and response objects.
  //
  // The chain requires the `url` and `method` fields, so they are included.
  const context = {
    url: `http://${hostname}:${port}${request.url}`,
    method: request.method,
    request,
    response,
  };

  // Pass the context object to the chain
  return chain
    .handle(context)
    .catch((error) => {
      if (context.url.includes("favicon")) {
        return response.end();
      }

      console.log(`Request URL hit an error: ${context.url}:\n`);
      console.log({ error });
      response.statusCode = 500;
      response.statusMessage = "Internal Server Error";
      response.end("Sorry, but we hit an error!");
    });
});

// Start the server
server.listen(port, hostname, () => {
  console.log(`\nDrash running at http://${hostname}:${port}`);
});
```

### Run the Drash app

```bash
npm start
```

_Note: `npm start` is defined in the `package.json` file._

### Open it

Go to http://localhost:1447. You should see something like the following:

```text
Oh so easy (written at Wed Nov 01 2023 22:04:56 GMT-0400 (Eastern Daylight Time))
```

</Steps>

This example is in the repository at [`examples/node-js-esm`](https://github.com/drashland/drash/tree/main/examples/node-js-esm).

  </Tabs.Tab>
  <Tabs.Tab>

## Steps

<Steps>

### Install Node

Install [Node](https://nodejs.org) (v20+).

### Install dependencies

Install the dependencies in `package.json`.

```bash
npm install
```

### Create the following file

```js filename="app.js"
const {
  Chain,
  Resource,
} = require("@drashland/drash/modules/http.polyfill");
const { createServer } = require("node:http");

// Create a resource
class Home extends Resource {
  paths = ["/"];

  GET(context) {
    console.log(`Received request: ${context.request.url}`);
    context.response.end(`Oh so easy (written at ${new Date()})`);
  }
}

// Build the chain and add the resource
const chain = Chain
  .builder()
  .resources(Home)
  .build();

// Define server variables for reuse below
const hostname = "localhost";
const port = 1447;

// Create the server
const server = createServer((request, response) => {
  const context = {
    url: `http://${hostname}:${port}${request.url}`,
    method: request.method,
    request,
    response,
  };

  // Pass the context object to the chain
  return chain
    .handle(context)
    .catch((error) => {
      if (context.url.includes("favicon")) {
        return response.end();
      }

      console.log(`Request URL hit an error: ${context.url}:\n`);
      console.log({ error });
      response.statusCode = 500;
      response.statusMessage = "Internal Server Error";
      response.end("Sorry, but we hit an error!");
    });
});

// Start the server
server.listen(port, hostname, () => {
  console.log(`\nDrash running at http://${hostname}:${port}`);
});
```

The only differences from the ESM variant are the `require()` calls, and that the polyfill specifier drops its `.js` extension.

### Run the Drash app

```bash
npm start
```

_Note: `npm start` is defined in the `package.json` file._

### Open it

Go to http://localhost:1447. You should see something like the following:

```text
Oh so easy (written at Wed Nov 01 2023 22:04:56 GMT-0400 (Eastern Daylight Time))
```

</Steps>

This example is in the repository at [`examples/node-js-cjs`](https://github.com/drashland/drash/tree/main/examples/node-js-cjs).

  </Tabs.Tab>
  <Tabs.Tab>

## Steps

<Steps>

### Install Node

Install [Node](https://nodejs.org) (v20+).

### Install dependencies

Install the dependencies in `package.json`.

```bash
npm install
```

### Create the following file

```ts filename="app.ts"
import {
  Chain,
  Resource,
} from "@drashland/drash/modules/http.polyfill.js";
import type { RequestMethod } from "@drashland/drash/core/Types.js";
import { createServer, IncomingMessage, ServerResponse } from "node:http";

type NodeContext = {
  url: string;
  method: RequestMethod;
  request: IncomingMessage;
  response: ServerResponse;
};

// Create a resource
class Home extends Resource {
  public paths = ["/"];

  GET(context: NodeContext) {
    console.log(`Received request: ${context.request.url}`);
    context.response.end(`Oh so easy (written at ${new Date()})`);
  }
}

// Build the chain and add the resource
const chain = Chain
  .builder()
  .resources(Home)
  .build();

// Define server variables for reuse below
const hostname = "localhost";
const port = 1447;

// Create the server
const server = createServer((request, response) => {
  const context = {
    url: `http://${hostname}:${port}${request.url}`,
    method: request.method,
    request,
    response,
  };

  // Pass the context object to the chain
  return chain
    .handle(context)
    .catch((error) => {
      if (context.url.includes("favicon")) {
        return response.end();
      }

      console.log(`Request URL hit an error: ${context.url}:\n`);
      console.log({ error });
      response.statusCode = 500;
      response.statusMessage = "Internal Server Error";
      response.end("Sorry, but we hit an error!");
    });
});

// Start the server
server.listen(port, hostname, () => {
  console.log(`\nDrash running at http://${hostname}:${port}`);
});
```

`NodeContext` is the shape you hand the chain. Declaring it is what makes `context.request` and `context.response` resolve inside the resource.

### Run the Drash app

```bash
npm start
```

_Note: `npm start` is defined in the `package.json` file._

### Open it

Go to http://localhost:1447. You should see something like the following:

```text
Oh so easy (written at Wed Nov 01 2023 22:04:56 GMT-0400 (Eastern Daylight Time))
```

</Steps>

This example is in the repository at [`examples/node-ts`](https://github.com/drashland/drash/tree/main/examples/node-ts).

  </Tabs.Tab>
</Tabs>

## Notes

Resources only recognize absolute URLs. Node’s `request.url` returns only a path, so it must be converted to a full URL first. For example:

`/users/1` --> `http://localhost:1447/users/1`

Node uses the **polyfill** entry point, and its `node:http` server does not give you a Web `Request` — so the resource receives a **context object** that you build. The chain only requires a readable `url` and `method`; everything else on it is yours.

Because the resource writes to `context.response` directly, `GET()` returns nothing. That is fine here — the response is already committed to the socket by the time the chain resolves. It stops being fine once you wrap the resource in [middleware](/docs/middleware), which needs a return value to pass along.
