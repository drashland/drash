// Mirrors src/modules/middleware/. Each middleware has its main module file in
// this directory (ETag.ts) and its related files in a sibling directory
// (e_tag/). Both are documented on the one page named after the module file.
export default {
  "accept-header": "AcceptHeader",
  cors: "CORS",
  etag: "ETag",
  "rate-limiter": "RateLimiter",
};
