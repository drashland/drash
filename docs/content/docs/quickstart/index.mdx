---
title: Quickstart
description: Get a Drash HTTP application running in Deno, Node, Bun, or Cloudflare Workers.
---

# Quickstart

Drash ships no server. You build a chain of handlers and pass a request into it from whatever server your runtime already gives you (`Deno.serve()`, `Bun.serve()`, etc.) — so the setup differs only in how your runtime hands you that request.

Pick your runtime below, or walk through the build one step at a time.

## First Drash application? Start here.

### Learn it step by step

<Cards num={1}>
  <Cards.Card
    title="Step-by-step guide"
    href="/docs/tutorials/step-by-step-guide"
    arrow
  />
</Cards>

### Or let your agent learn it

Hand this to your coding agent:

```text
Read https://drash.crookse.com/agents.md and set up a Drash project.
```

## Or jump to your runtime

<Cards num={2}>
  <Cards.Card title="Deno" href="/docs/quickstart/deno" arrow />
  <Cards.Card title="Node" href="/docs/quickstart/node" arrow />
  <Cards.Card title="Bun" href="/docs/quickstart/bun" arrow />
  <Cards.Card
    title="Cloudflare Workers"
    href="/docs/quickstart/cloudflare-workers"
    arrow
  />
</Cards>

Each page has a complete, runnable application and the notes specific to that runtime.

| Runtime            | `URLPattern`                   | Request shape              | Entry point       |
| ------------------ | ------------------------------ | -------------------------- | ----------------- |
| Deno               | Global — used directly         | Web `Request` / `Response` | `http.native.js`   |
| Node               | Varies by version — polyfilled | Context object you build   | `http.polyfill.js` |
| Bun                | Not dependable — polyfilled    | Web `Request` / `Response` | `http.polyfill.js` |
| Cloudflare Workers | Global — used directly         | Web `Request` / `Response` | `http.native.js`   |

The two entry points differ only in which `URLPattern` implementation they use — see [Concepts > Native vs. Polyfill](/docs/concepts/native-vs-polyfill) to learn more.

## Before you start

If you do not have a runtime installed yet, or have never stood up an HTTP server in it, read [Prerequisites](/docs/getting-started/prerequisites) first.
