# Handling Requests

What a resource receives, and how to read from it

## Overview

Every chain built by the HTTP module's `Application.builder()` runs a `RequestParamsParser` handler before it calls your resource. That handler attaches a non-enumerable `params` object to the request, and that object is how you read the two kinds of parameters a URL can carry:

- __path params__ &mdash; the `:name` segments declared in a resource's `paths` property; and
- __query params__ &mdash; the `?name=value` pairs a client appends to the URL.

You do not add the parser yourself. It is one of the handlers the request chain wires up for you, so `params` is present on every request that reaches a resource.

### Recommended Reading

- Read the concepts on [HTTP applications](/about/concepts/http-application)
- Read [Creating a Resource](/docs/resources/creating-a-resource) to see how paths are declared

### Objectives

To gain familiarity with:

- what a resource's HTTP methods receive;
- typing the request so your editor knows `params` exists; and
- the two methods `params` provides.

## What a Resource Receives

An HTTP method receives whatever was passed to `app.handle()`. On runtimes with Web `Request`/`Response` that is the `Request` object itself. On Node it is the context object you built. In both cases the chain has already attached `params` to it by the time your method runs.

## Typing the Request

A `params` field is attached to the request at runtime. In TypeScript projects, a plain `Request` type does not know about it. Use the `HTTPRequest` type (exported by the HTTP module) and your editor will recognize the `params` field&mdash;enabling autocompletion on it:

<CodeTabs
  items={["Node", "Deno", "Bun", "Cloudflare Workers"]}
>
<CodeTab>

```js
import { Resource } from "@drashland/drash/modules/http.polyfill.js";

class Users extends Resource {
  paths = ["/users/:id?"];

  // `HTTPRequest` types a Web `Request`, so it does not apply here — Node hands
  // the chain a context object and `params` is attached to that instead.
  GET(context) {
    const id = context.params.pathParam("id");
    const sort = context.params.queryParam("sort");

    context.response.end(`id=${id} sort=${sort}`);
  }
}
```

</CodeTab>
<CodeTab>

```ts {3,9-10}
import {
  Resource,
  type HTTPRequest,
} from "npm:@drashland/drash/modules/http.native.js";

class Users extends Resource {
  public paths = ["/users/:id?"];

  public GET(request: HTTPRequest) {
    // `request.params` is known to the type checker here
    const id = request.params.pathParam("id");
    const sort = request.params.queryParam("sort");

    return new Response(`id=${id} sort=${sort}`);
  }
}
```

</CodeTab>
<CodeTab>

```ts {3,9-10}
import {
  Resource,
  type HTTPRequest,
} from "@drashland/drash/modules/http.polyfill.js";

class Users extends Resource {
  public paths = ["/users/:id?"];

  public GET(request: HTTPRequest) {
    // `request.params` is known to the type checker here
    const id = request.params.pathParam("id");
    const sort = request.params.queryParam("sort");

    return new Response(`id=${id} sort=${sort}`);
  }
}
```

</CodeTab>
<CodeTab>

```js {6-8}
import { Resource } from "@drashland/drash/modules/http.native.js";

class Users extends Resource {
  paths = ["/users/:id?"];

  // Cloudflare is in plain JavaScript, so no typing goes here.
  // Just access the `request.params` field. It should already be attached.
  GET(request) {
    const id = request.params.pathParam("id");
    const sort = request.params.queryParam("sort");

    return new Response(`id=${id} sort=${sort}`);
  }
}
```

</CodeTab>
</CodeTabs>

## The `params` Object

It provides exactly two methods:

| Method             | Returns               | Notes                                     |
| ------------------ | --------------------- | ----------------------------------------- |
| `pathParam(name)`  | `string \| undefined` | From the matched `URLPattern`             |
| `queryParam(name)` | `string \| undefined` | From the query string — see [Query Params](/docs/requests/query-params) |

Both return `undefined` for a name that was not matched rather than throwing. That is what makes optional params usable: you check the value instead of catching an error.

```ts
const id = request.params.pathParam("id");

if (!id) {
  throw new HTTPError(Status.BadRequest, "id is required");
}
```

`params` is defined as non-enumerable, so it does not show up in `Object.keys(request)` or `JSON.stringify(request)`. It is there &mdash; it just stays out of the way of anything that walks the request's own properties.
