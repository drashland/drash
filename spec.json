{
    "info": {
        "title": "My API",
        "version": "v1.0.1"
    },
    "paths": {
        "/users/{user_id}/accounts/{account_id}": {
            "get": {
                "parameters": [
                    {
                        "in": "header",
                        "name": "X-TEST",
                        "description": "Some random header. Who knows?",
                        "type": "string"
                    }
                ],
                "responses": {
                    "200": {
                        "description": "If you got it!"
                    },
                    "400": {
                        "description": "If you bad requested it!"
                    },
                    "404": {
                        "description": "If we no find!"
                    },
                    "500": {
                        "description": "Daaaaang... got'em!"
                    }
                }
            },
            "post": {
                "parameters": [
                    {
                        "in": "body",
                        "name": "Body Payload",
                        "description": "Some body.",
                        "schema": {
                            "type": "object",
                            "properties": {
                                "action": {
                                    "type": "string"
                                },
                                "write": {
                                    "type": "boolean"
                                }
                            },
                            "required": [
                                "action"
                            ]
                        }
                    }
                ],
                "responses": {
                    "200": {
                        "description": "Test POST"
                    }
                }
            },
            "parameters": [
                {
                    "in": "path",
                    "name": "account_id",
                    "description": "The ID of the account to get.",
                    "type": "number",
                    "required": true
                },
                {
                    "in": "path",
                    "name": "user_id",
                    "description": "The ID of the user who owns the account.",
                    "type": "number",
                    "required": true
                }
            ]
        }
    },
    "swagger": "2.0"
}